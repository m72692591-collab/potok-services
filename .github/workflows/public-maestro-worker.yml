name: ARCHIVED — public MAESTRO worker disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-only:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Historical marker
        run: echo 'This worker is permanently disabled; potok-services is legacy redirect only.'

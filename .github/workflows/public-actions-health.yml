name: ARCHIVED — public worker health disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-only:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Historical marker
        run: echo 'Health mutation is disabled; only the legacy redirect validator remains active.'

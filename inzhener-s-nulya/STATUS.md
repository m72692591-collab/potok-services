# Статус

Дата проверки: 2026-09-26.

Статус: **ЧАСТИЧНО — БЛОКИРОВКА CAPTCHA/SECURITY CHECK REPLIT**.

## ЗАДАЧА

Перенести существующий проект «Инженер с нуля» на Replit Starter, сохранив Т‑Бизнес, НПД, цены, юридические страницы и защищённую выдачу без показа покупателю `*.vercel.app`.

## СДЕЛАНО

- Клонирован канонический `main` репозитория `m72692591-collab/potok-services`; работа ведётся в существующей папке `inzhener-s-nulya`.
- Установлено фактическое устройство проекта: статические HTML‑страницы и Vercel‑совместимые Node.js API; прежней build/start команды для отдельного сервера не было.
- Добавлен Replit‑совместимый Node.js сервер и корневой `.replit` с build/start командами.
- Сохранены цены 1 990/2 490/3 490 ₽ и все юридические маршруты.
- Исходники, `.env` и `package.json` не отдаются публичным сервером.
- Усилена проверка Т‑Бизнеса: `CONFIRMED` принимается только после авторитетного `GetState` и точного совпадения `Success`, `ErrorCode`, `TerminalKey`, `OrderId`, `PaymentId` и суммы. Webhook не создаёт чек НПД по одному уведомлению банка.
- Локально проверены главная, юридические страницы и API. На viewport 390×844 нет горизонтального переполнения; три цены и кнопки отображаются, 14 ссылок на юридические документы доступны.
- Проверены актуальные официальные условия Replit: Starter позволяет опубликовать один app; Replit выдаёт бесплатный `*.replit.app`; custom domain требует принадлежащий владельцу домен. Billing и платные функции не включались.
- Открыт импорт Replit, но Cloudflare показал обязательную проверку «что вы не бот». CAPTCHA/проверка не проходилась автоматически.

## РЕЗУЛЬТАТ

Код подготовлен и локально проверен для Replit. Публичный Replit App не создан и не опубликован, production‑секреты не перенесены. Покупательский путь на Replit поэтому ещё не существует.

## ДОКАЗАТЕЛЬСТВО

- `npm ci` → 0 vulnerabilities.
- `npm run check` → 4/4 теста прошли.
- Локальные маршруты `/`, `/offer`, `/privacy`, `/payment`, `/delivery`, `/return`, `/contacts`, `/requisites` → HTTP 200.
- Мобильная проверка 390×844 → `overflowX=false`; цены `1 990 ₽`, `3 490 ₽`, `2 490 ₽`; кнопки шириной 299–301 px.
- Replit `/import` → страница Cloudflare «Выполнение проверки безопасности», Ray ID `a410ea946aabf516`.
- Попытка проверить прежний `https://inzhener-s-nulya.vercel.app/api/readiness` из текущей сети завершилась тайм-аутом; исторический статус 2026-09-25 не считается текущей production‑проверкой.
- Поиск по текущей ветке и истории не нашёл Google Drive; фактический `api/download.js` использует приватный `@vercel/blob`.

## ОСТАЛОСЬ

1. Владелец проходит показанную Replit/Cloudflare проверку в оставленной вкладке.
2. Codex импортирует репозиторий, проверяет тариф Starter и отсутствие billing, затем добавляет production‑значения только в Replit Secrets.
3. Codex публикует один app, записывает `APP_PUBLIC_URL`, повторяет deploy и проверяет публичные маршруты.
4. Codex начинает заказ каждого товара без оплаты и сверяет сумму, домен формы Т‑Бизнеса, success/fail/webhook URL.
5. Codex подтверждает фактический контур выдачи. Если владелец действительно использует Google Drive вне репозитория, требуется обнаружить его в аккаунте и интегрировать без публичного доступа; иначе сохраняется текущий приватный Blob.

Реальный платёж и реклама запрещены.

# Статус

Дата проверки: 2026-09-25.

Статус: **БЛОКИРОВКА**.

## ЗАДАЧА

Опубликовать бесплатную витрину «Инженер с нуля» в Яндекс Бизнесе и сохранить действующий путь покупки через Т-Бизнес/backend.

## СДЕЛАНО

- Найден канонический проект и сохранены текущие незакоммиченные сценарии сборки для Yandex Object Storage.
- Проверен действующий сайт `https://inzhener-s-nulya.vercel.app`.
- Проверен публичный `/api/readiness`: production T-Бизнес, секрет заказа, закрытое хранилище и автоматизация НПД отмечены готовыми; `launchReady=true`.
- Проверен `/api/public-info`: реквизиты продавца и контакты доступны.
- SEO title и description приведены к утверждённым значениям.
- Подготовлена точная карта переноса в `YANDEX_BUSINESS_CONTENT.md`.
- Зафиксировано ограничение Яндекс Бизнеса: встроенный сайт формируется из полей профиля, не позволяет добавлять произвольные разделы и использует встроенный, нередактируемый счётчик Метрики.
- После действия владельца «готово» повторно проверен Computer Use: браузерный мост всё ещё возвращает `Unable to load browser request-header policy`, список браузеров пустой.
- Проверена статическая сборка `npm --prefix inzhener-s-nulya run build:yandex`: сборка проходит и создаёт `dist-yandex`; артефакт исключён из Git.
- С ключом сервисного аккаунта `inzhener-site-deploy` создан bucket `inzhener-s-nulya-1790344532628`, загружена статическая сборка и включён режим статического сайта.
- Внешняя проверка `https://inzhener-s-nulya-1790344532628.website.yandexcloud.net` вернула HTTP 403: bucket остаётся приватным.

## РЕЗУЛЬТАТ

Существующая торговля не изменена. Публичная витрина Яндекс Бизнеса не опубликована. Статическая заготовка в Yandex Object Storage загружена, но публично недоступна из-за закрытого доступа bucket.

## ДОКАЗАТЕЛЬСТВО

- `GET https://inzhener-s-nulya.vercel.app` → HTTP 200.
- `GET https://inzhener-s-nulya.vercel.app/api/readiness` → HTTP 200, `paymentProvider=tbank`, `environment=production`, `npdConnected=true`, `launchReady=true`.
- `GET https://inzhener-s-nulya.vercel.app/api/public-info` → HTTP 200.
- Управляемый браузер трижды вернул `Unable to load browser request-header policy`; после сброса сессии ошибка повторилась, список доступных браузеров остался пустым.
- После перезапуска/проверки владельцем ошибка Computer Use повторилась с тем же текстом; `cua.listWindows()` в текущем рантайме недоступен.
- Локальные переменные `YC_ACCESS_KEY_ID` и `YC_SECRET_ACCESS_KEY` отсутствуют; `yc`, AWS CLI и профиль AWS не установлены.
- `npm --prefix inzhener-s-nulya run build:yandex` → `Built ...\inzhener-s-nulya\dist-yandex`.
- `node scripts/deploy-yandex-s3.mjs` с `YC_BUCKET=inzhener-s-nulya-1790344532628` → вернул `websiteUrl`.
- `GET https://inzhener-s-nulya-1790344532628.website.yandexcloud.net` → HTTP 403.
- `PutBucketPolicy` для публичного `s3:GetObject` → HTTP 403 `AccessDenied`.

## ОСТАЛОСЬ

1. Добавить сервисному аккаунту `inzhener-site-deploy` роль `storage.admin` на каталог либо вручную открыть публичный доступ на чтение объектов в bucket `inzhener-s-nulya-1790344532628`.
2. Повторить настройку публичной bucket policy и внешнюю проверку URL.
3. Восстановить управляемый доступ к уже авторизованному браузеру.
4. Открыть точную организацию в Яндекс Бизнесе и проверить права/подтверждение компании.
5. Внести данные из `YANDEX_BUSINESS_CONTENT.md`, не запуская магазин/корзину и рекламу.
6. Настроить бесплатный промежуточный российский URL либо доказать доступный способ перехода, который не показывает `*.vercel.app` и не ломает согласие с офертой, контакт покупателя и возврат на страницу заказа.
7. Только после этого опубликовать и выполнить мобильную и сквозную проверку.

Публикация до выполнения пункта 4 запрещена: прямой переход из карточки на Vercel нарушает заданное требование, а статическая сборка Object Storage намеренно закрывает продажи, поскольку не содержит проверенного серверного прокси к текущему backend.

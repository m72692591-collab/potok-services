import crypto from 'node:crypto';import{CATALOG,baseUrl,json,readJson,requirePayKey,signOrder,validateContact,yandexApiBase}from'./_shared.js';export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'method_not_allowed'});try{const b=await readJson(req),p=CATALOG[String(b.product||'').toLowerCase()],contact=validateContact(b.contact);if(!p)return json(res,400,{error:'invalid_product'});if(!contact)return json(res,400,{error:'invalid_contact'});const orderId=`IZN-${p.code.toUpperCase()}-${crypto.randomUUID()}`,token=signOrder(orderId,p.code),site=baseUrl(req),orderPage=`${site}/order.html?orderId=${encodeURIComponent(orderId)}&product=${p.code}&token=${encodeURIComponent(token)}`;const item={productId:p.code,title:p.title,quantity:{count:'1'},unitPrice:p.price.toFixed(2),subtotal:p.price.toFixed(2),total:p.price.toFixed(2)};const tax=process.env.YANDEX_RECEIPT_TAX_CODE;if(tax&&/^\d+$/.test(tax))item.receipt={tax:Number(tax)};const payload={orderId,currencyCode:'RUB',orderSource:'WEBSITE',isPrepayment:true,fiscalContact:contact,purpose:p.title,ttl:1800,redirectUrls:{onSuccess:`${orderPage}&result=success`,onError:`${orderPage}&result=error`,onAbort:`${orderPage}&result=abort`},cart:{externalId:orderId,items:[item],total:{amount:p.price.toFixed(2)}}};const r=await fetch(`${yandexApiBase()}/orders`,{method:'POST',headers:{Authorization:`Api-Key ${requirePayKey()}`,'content-type':'application/json'},body:JSON.stringify(payload)}),d=await r.json().catch(()=>({}));if(!r.ok||d?.status!=='success'||!d?.data?.paymentUrl)return json(res,502,{error:'payment_provider_error'});return json(res,200,{paymentUrl:d.data.paymentUrl})}catch(e){const cfg=/YANDEX_PAY_API_KEY|ORDER_HMAC_SECRET/.test(String(e?.message||e));return json(res,cfg?503:500,{error:cfg?'not_configured':'server_error'})}}